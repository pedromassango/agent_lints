// Analyzer plugin entry point. The analysis server loads this file and reads
// the top-level `plugin` variable.
import 'package:agent_lint/src/plugin/agent_lint_plugin.dart';

final plugin = AgentLintPlugin();
